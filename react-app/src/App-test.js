// import { render, screen } from '@testing-library/react'
// import App from './App'

// test('renders learn react link', () => {
//   render(<App />)
//   const linkElement = screen.getByText(/learn react/i)
//   expect(linkElement).toBeInTheDocument()
// })

// test('3 list item', () => {
//   render(<App />)
//   const list = screen.getAllByRole("listitem")
//   expect(list).toHaveLength(3)

//   // expect(list.length).toBe(3)
//   // expect(list.length).toEqual(3)
// })

// test('render title', () => {
//   render(<App />)
//   const title = screen.getByTestId("title-test")
//   expect(title).toBeInTheDocument()

//   // expect(list.length).toBe(3)
//   // expect(list.length).toEqual(3)
// })

// test('sum to be 6', () => {
//   render(<App />)
//   const span = screen.getByTitle("sum")
//   // expect(span.textContent).toBe(6) throw error
//   expect(span.textContent).toBe("6")
// })

// // error if duplicate title name

// test('sumTwo to be 6', () => {
//   render(<App />)
//   const span = screen.getByTitle("sumTwo")
//   // expect(span.textContent).toBe(6) throw error
//   expect(span.textContent).toBe("6")
// })
